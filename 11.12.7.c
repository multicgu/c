#include<stdio.h>
#include<string.h>
#define M1 "How are ya, sweetie? "
char M2[40] = "Beat the clock.";
char * M3 = "chat";
int main(void)
{
	char words[80];	
	printf(M1);
/*	puts(M1);
	puts(M2);
	puts(M2+1);
	strcpy(words,M2);
	strcat(words," Win a toy.");
*/	
	return 0;
}
